## User Channels

<!-- example request -->
```shell
curl -i -H 'X-Apikey: [OPTIONAL_APIKEY]' <%= current_page.data.api_url %>/users/1/channels
```

<!-- example response -->
<%= partial 'partials/responses/channels' %>

Returns a paginated array of all channels created by the requested user.

### HTTP Request

`GET <%= current_page.data.api_url %>/users/:id/channels`

<aside class="notice">
When authenticated with an X-Apikey header and the apikey matches the requested user’s apikey any channels marked as hidden by that user will be included in the response.
</aside>

### Query Parameters

Parameter | Default | Description
--------- | ------- | -----------
page      |         | specific page of results
per_page  | 30      | number of results per page

### Error Responses

Code | Message
---- | -------
404  | Couldn't find User with 'id'=