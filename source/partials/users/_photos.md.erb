## User Photos

<!-- example shell request -->
```shell
curl -i -H 'X-Apikey: [OPTIONAL_APIKEY]' <%= current_page.data.api_url %>/users/1/photos
```

<!-- example response -->
<%= partial 'partials/responses/photos' %>

Returns a paginated array of all photos created by the requested user.

### HTTP Request

`GET <%= current_page.data.api_url %>/users/:id/photos`

<aside class="notice">
When authenticated with an X-Apikey header an additional property, `stared`, will be included in each of the photo objects in the array indicating whether the currently authenticated user has stared that photo.
</aside>

<aside class="notice">
When authenticated with an X-Apikey header and the apikey matches the requested user’s apikey any photos marked as hidden by that user will be included in the response.
</aside>

### Query Parameters

Parameter | Default | Description
--------- | ------- | -----------
id        |         | user's integer ID or slug
page      |         | specific page of results
per_page  | 30      | number of results per page

### Error Responses

Code | Message
---- | -------
404  | Couldn't find User with 'id'=