## Unstar Photo

<!-- example shell request -->
```shell
curl -i -XDELETE -H 'X-Apikey: [REQUIRED_APIKEY]' <%= current_page.data.api_url %>/photos/1/star
```

<!-- example response -->
<%= partial 'partials/responses/unstar' %>

Unstar the requested photo.

### HTTP Request

`DELETE <%= current_page.data.api_url %>/photos/:id/star`

<aside class="warning">
An X-Apikey header containing a valid API key is required to access this endpoint.
</aside>

### Error Responses

Code | Message
---- | -------
401  | A valid apikey is required to access this endpoint
404  | Couldn't find Photo with 'id'=