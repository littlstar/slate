## Category Videos

<!-- example request -->
```shell
curl -i -H 'X-Apikey: [OPTIONAL_APIKEY]' <%= current_page.data.api_url %>/categories/1/videos
```

<!-- example response -->
<%= partial 'partials/responses/videos' %>

Returns a paginated array of all videos in the requested category.

### HTTP Request

`GET <%= current_page.data.api_url %>/categories/:id/videos`

<aside class="notice">
When authenticated with an X-Apikey header an additional property, `stared`, will be included in each of the video objects in the array indicating whether the currently authenticated user has stared that video.
</aside>

<aside class="notice">
The "download" version will only be returned if the video owner has enabled downloads.
</aside>

### Query Parameters

Parameter | Default | Description
--------- | ------- | -----------
page      |         | specific page of results
per_page  | 30      | number of results per page

### Error Responses

Code | Message
---- | -------
404  | Couldn't find Category with 'id'=