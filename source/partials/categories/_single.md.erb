## Single Category

<!-- example request -->
```shell
curl -i <%= current_page.data.api_url %>/categories/1
```

<!-- example response -->
<%= partial 'partials/responses/category' %>

Returns the details for the requested category.

### HTTP Request

`GET <%= current_page.data.api_url %>/categories/:id`

### Error Responses

Code | Message
---- | -------
404  | Couldn't find Category with 'id'=